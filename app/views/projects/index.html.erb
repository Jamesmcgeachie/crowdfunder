<% @projects.each do |project| %>
	<p><%= link_to "#{project.name}", "/projects/#{project.id}" %>, <%= project.description %>, <%= project.funding_goal %>, <%= project.start_date %>, <%= project.end_date %> 
	</p>
<% end %>